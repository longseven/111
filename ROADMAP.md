# 优化路线图（下一步方向与途径）

本文件记录 K12 数学题目改编系统的后续优化方向、价值与**具体实现途径**，按对"出卷"场景的价值排序。

> **进度**：①②③④⑤ 均已落地（见各节"✅ 已完成"）。后续如有新方向继续追加。

---

## 当前已实现（基线）

上传题目 → 四步流水线（① 识别原题 / ② 改编生成 / ③ 不超纲校验 / ④ 答案校验）→ 结果展示/在线编辑 →
导出 Word（图片公式 / 可编辑 OMML）/ 存入题库。
已有：进度条、改编理由逐条"原→新"、选择题选项分行、失败可见、请求超时、校验失败不丢结果、题目预览、
整卷拆分并发改编、题库、在线编辑、网页内设置、可选访问口令、Docker 部署。

---

## 优先级 ①：答案正确性校验 ⭐（出卷命根子）  ✅ 已完成

> 已落地方案1：独立解题复核 `verify_answer`（全新上下文重解，不给原答案）→ 比对盖"答案已复核 ✓"/标红"答案存疑"并给出正确答案。前端单题流程并行执行不超纲与答案校验。

**问题**：当前只校验"不超纲"，未校验"答案是否正确"。模型解难题可能算错，错答案上卷子风险最高。

**途径（由轻到重，可组合）**

1. **独立解题复核（LLM）— 建议先做**
   - 新增校验步 `verify_answer`：把"新题干"单独发给模型（全新上下文、不提供原答案），独立重解 → 得"复核答案"。
   - 与生成时的 `answer` 比对：一致 → 盖"答案已复核 ✓"；不一致 → 标红"答案存疑"并**自动重生成该题**（最多重试 N 次）。
   - 结构同 `verify_in_scope`，再加一次独立调用。
2. **数值题代码验算**：让模型给出 `sympy` 验算代码，在 sandbox 跑一遍核对数值答案。最可靠，但仅覆盖可计算题。
3. **自洽性检查**：解析步骤与最终答案是否一致；题干数据是否矛盾/无解。

**涉及文件**：`app/claude_service.py`（加 `verify_answer`）、`app/prompts.py`、`app/schemas.py`、`app/main.py`（加 `/api/verify_answer` 或并入流程）、前端徽章。

---

## 优先级 ②：整卷 / 批量改编  ✅ 已完成

> 已落地：`/api/split` 模型拆题 → 整卷模式逐题改编（并发限流）→ `/api/export_paper` 组卷导出 Word。

**目标**：上传一整张试卷 → 一次产出整张新卷。

**途径**
- PDF 多题切分：模型识别题号/题目边界，拆成题目列表。
- 逐题（并行）改编，复用现有 parse/adapt/verify。
- **组卷导出**：带题号、分值、答题留白的整卷 Word/PDF 模板。

---

## 优先级 ③：生成提速与成本  ✅ 已完成（流式留作后续）

> 已落地：单题不超纲/答案校验 `Promise.all` 并行；整卷多题并发限流（`runLimited`，并发 3，单题失败不中断整卷）；Anthropic system prompt 加 ephemeral 提示缓存。流式输出（SSE）留作后续增强。

**途径**
- **流式输出（SSE）**：边生成边显示，免干等。（待做）
- **多题并行**：N 道拆成 N 个小请求并行 → 更快、更不易因长输出被截断。✅
- **提示缓存**：原题解析 + 系统提示在多变体/多次请求间缓存（Claude 原生支持），省时省钱。✅

---

## 优先级 ④：题库与在线编辑  ✅ 已完成

> 已落地：`app/store.py`（标准库 sqlite3）+ `/api/bank/*`，前端「★ 存入题库」/题库列表/调回/删除；结果区每题「编辑」内联改题干/答案/解析/理由/知识点/难度，改完即用于导出与存库。从题库挑题组卷可作后续增强。

**途径**
- 轻量 SQLite 存生成历史 / 收藏。✅
- 结果页可直接微调题干、答案后再导出。✅
- 从题库挑题组卷。（待做）

---

## 优先级 ⑤：工程化 / 部署  ✅ 已完成

> 已落地：网页内「⚙ 设置」在线切 provider/模型/key（运行时覆盖文件，即时生效）；Dockerfile + docker-compose（data 卷持久化）；可选 `APP_PASSWORD` 访问口令（HMAC 派生 Cookie）。

**途径**
- 网页内切换模型 / 填 key（免改 `.env`）。✅
- Docker 一键部署。✅
- 多人使用时加鉴权。✅（共享口令；如需按账号细分可后续扩展）

---

## 免费即时项

把 `.env` 的 `OPENAI_MODEL` 从 `gpt-5.4` 换成 `claude-opus-4-8`（codexzh 目录里有的话）：质量上限直接提一档，无需改代码。

---

## 建议执行顺序（已完成）

1. ✅ **答案正确性校验**（独立解题复核）— 覆盖广、见效快、对出卷最关键。
2. ✅ **整卷批量改编 + 组卷导出** — 效率倍增。
3. ✅ 生成提速（并行/缓存；流式待做）。
4. ✅ 题库与在线编辑。
5. ✅ 工程化（Docker / 在线设置 / 访问口令）。

## 后续可选增强

- 生成流式输出（SSE）边出边显。
- 数值题 `sympy` 代码验算（最可靠的答案核验）。
- 从题库挑题自由组卷、生成时不一致自动重生成。
- 按账号细分的多用户鉴权与配额。
